# frozen_string_literal: true
source "https://rubygems.org"

gem "jekyll", "~> 4.4"
gem "webrick" # required to serve locally on Ruby 3+
gem "jekyll-seo-tag" # Open Graph / Twitter Card / JSON-LD meta tags

# Standard-library gems no longer bundled with newer Ruby
gem "csv"
gem "bigdecimal"
